# План 15–29.09.2026 и гейты

Дедлайн подачи — 29.09.2026 23:59 МСК, отправка не позднее 20:00.
Бюджет времени: 70% конкурсный E2E и пороги, 20% evaluation/reliability,
10% питч. Всё вне critical path режется.

Состояние на 16.09.2026 (вечер, после импорта матрицы):

- гейт **A частично закрывается данными**: Приложения 1 и 2 найдены; SHA-256
  архивов и артефакт отправки Q&A по-прежнему отсутствуют;
- гейт **B/G сдвинуты кодом**: 132 schema-valid правила, адаптер submission,
  allowlist TRAIN_PUBLIC; fail-closed по каскаду L1–L7, finalize только
  человеком, GOLD-индекс по evidence_group. Покрытие — `extractor_missing`.
- **подтверждённая утечка по структуре, не по коду**: каталог
  `01_ПАКЕТ_УЧАСТНИКАМ_3_ОБЪЕКТА\...\01_ДОКУМЕНТАЦИЯ\Речников ул. 7-7`
  содержит 213 документов объекта `OBJ-RECHNIKOV-7-7` — того самого, что
  объявлен `TEST_HIDDEN` в `split_policy.json`. Документы **без меток**
  (UNLABELED, ORGANIZER_ONLY), то есть ответов не раскрывают, но corpus
  скрытого теста лежит в открытом пакете. Правило: для обучения, подбора
  порогов, prompt и препроцессинга используются **только**
  `OBJ-NOVOSLOBODSKAYA` и `OBJ-TYUMENSKAYA-5-GOLD-SEED` (TRAIN_PUBLIC);
  `Речников ул. 7-7` обрабатывается ровно как будущий тест — через pipeline,
  без обратной связи.

## Что поставка изменила в фактах

- Приложение 1: 132 параметра с полями `parameter_code` (`PZ-001` … `SM-132`),
  `unit` (131 числовой/единичный, 0 без единицы), `source_pd/rd/id`, `trigger`,
  `criticality` (106 критических, 26 существенных). Это источник генерации
  `data/matrix/`, а не ручного ведения.
- Приложение 2: протокол с резолютивной частью (предписания по каждой
  находке); короткие коды в тексте (`AR-14`, `KR-55`) — отображаемое сокращение,
  канонический код — из каталога (`AR-014`, `KR-055`).
- Скоринг (100 баллов): F1 детекции 60, точная локализация файл+страница 15,
  нормализованное значение и статус 15, целостность/split-handling 10.
  **Гейт**: пропуск любой утверждённой критической точки ограничивает итог
  59/100. Recall по критическим — приоритет №1, что перетягивает баланс
  precision-first из AGENTS.md для кандидатов.
- Паспорт разметки v0.1: из 26 141 страницы подтверждены только 3 нарушения
  и 0 отрицательных примеров; всё остальное — UNLABELED и **не является**
  отрицательным классом. `public_gold_checks.jsonl` даёт 15 проверок
  (10 VIOLATION_PRESENT / 5 NO_VIOLATION), NO_VIOLATION — только явно
  экспертно подтверждённые пары.
- Золотые нарушения привязаны к кодам: IOS4-079 (конфигурация вентустановок),
  IOS4-078 (вентиляция/воздуховоды), FREE-HEATING-001 (тёплые полы — **пробел
  матрицы**, параметра нет среди 132). Матрица должна уметь выражать
  free-search находку вне реестра.

## Гейты

| Гейт | Дата | Условие прохода |
|---|---|---|
| A | 15.09 | Карантин test_213 в политике; SHA-256 всех архивов; inventory; вопросы отправлены **и подтверждены артефактом** |
| B | 16.09 | Ответы Q&A зафиксированы как ADR; заморожены API, статусы, схема evidence_group, Rule DSL |
| C | 17.09 | Document passport: Exact Match ключевых полей ≥0,92 на validation v0 |
| D | 18.09 | 100% synthetic coordinate tests; локализация ≥0,95 |
| E | 19.09 | Revision resolver: связка ≥0,97, stale-revision FPR ≤0,05 |
| F | 20.09 | Сценарии FULL/PD_RD_ONLY/…/PARTIALLY_LOADED; отсутствие стадии не даёт violation |
| G | 21.09 | 132/132 строк матрицы — schema-valid, с applicability, sources, operator, test skeleton; **генерация из Приложения 1 + `parameter_catalog_132.jsonl`, коды `PZ-001`-формата** |
| H | 22–23.09 | ≥20 атомарных правил проходят E2E; ни одной предметной находки без evidence |
| I | 24.09 | OCR bake-off: выбран production primary + независимый verifier; CA ≥0,97 |
| J | 25.09 | Калибровка: P≥0,93, R≥0,83, F1≥0,88, FPR≤0,07 на validation; **отдельно: recall по 106 критическим параметрам = 1,00 (иначе скоринг-гейт 59/100)** |
| K | 26.09 | UI ≤3 кликов на находку; протокол по Приложению 2 **(образец найден)**; dry run 132 параметра ≤30 мин |
| L | 27.09 | `/status` p95 ≤200 мс; 100 виртуальных пользователей; retry не даёт дублей |
| M | 28.09 | Все минимумы ТЗ достигнуты; 0 critical demo defects; записано резервное видео |
| N | 28.09 | Adversarial readiness (см. `RED_TEAM.md`) |

## Stop-conditions

- 16.09 обнаружена утечка ответов скрытого теста (не документов, а разметки) →
  полный стоп, фиксация, вопрос организатору.
- 18.09 нет корректных координат → остановить работу над моделями, чинить геометрию.
- 20.09 связка <0,95 → не расширять число правил.
- 24.09 OCR <0,95 → усиливать region crops и классический OCR, не дообучать большую VLM.
- 25.09 Precision <0,90 → поднимать abstention, а не recall; **исключение —
  критические параметры: там abstention недопустим сверх бюджета скоринг-гейта**.
- 25.09 F1: гармоническое среднее порогов P=0,90 и R=0,80 ≈ 0,847 < 0,85.
  Точка на полу precision и recall **не** закрывает F1. Запас — по precision.
- 27.09 нет протокола по Приложению 2 → немедленно делать protocol adapter.

## Расхождения контрактов (адаптер, домен не трогаем)

Внешний контракт соревнования (`contracts/schemas/submission.schema.json`)
систематически отличается от внутренних схем — маппинг в
`kontur.evaluation.submission`:

1. **Статусы**: внешние `VIOLATION_PRESENT / NO_VIOLATION /
   MISSING_DOCUMENT / COMPARISON_IMPOSSIBLE` и `protocol_status: OK /
   WARNING / CRITICAL / ID_MISSING / RD_MISSING / PD_MISSING /
   COMPARISON_IMPOSSIBLE` ↔ внутренние `FindingStatus` (11 значений) и
   `review_priority` (HIGH/MEDIUM/LOW). Маппинг — отдельный адаптер;
   домен не правится под внешний enum.
2. **Коды**: внешний канон `PZ-001`/`IOS4-079`; короткие формы (`PZ-01`,
   `AR-14`) нормализуются `canonicalize_rule_code`. `FREE-HEATING-001` —
   `data/matrix/free_search.json`, не 133-я строка матрицы.
3. **Evidence**: внешний минимум — `(stage, file_id, pdf_page_number)`;
   внутренний инвариант строже (SHA-256, polygon source+norm). Внешняя
   схема — подмножество, экспорт тривиален; импорт внешних меток во
   внутренний формат требует догрузки хешей и геометрии.
4. **Критичность**: gold `criticality` (Критическое/Существенное) ≠
   `review_priority`. Это разные оси: юридическая тяжесть и очередь
   проверки; хранить обе, не смешивать.
5. **ИД-тройственность**: `stage: PD/RD/ID` совпадает, но `document_status:
   PD_RD_AVAILABLE_ID_NOT_REQUIRED_FOR_PAIRWISE_CHECK` подтверждает:
   попарная проверка ПД↔РД валидна без ИД — отсутствие стадии не нарушение
   (инвариант 4 соблюдается и в данных организатора).

## Порядок сборки вертикальных слайсов

Каждое правило проходит полный путь `source → extraction → compare → evidence →
review → protocol`, прежде чем берётся следующее. Горизонтальная разработка
«сначала все экстракторы» запрещена: она не даёт демонстрируемого результата.

Первые правила — по кодам каталога, с учётом золотых нарушений и типов
компараторов:

1. `PZ-001` (площадь застройки, м²) — число с допуском; триггер «расхождение
   контуров > 0». Источники: ПЗУ ТЭП ↔ ПП «Общие данные» ↔ техплан БТИ.
2. `KR-055` (класс бетона, Марка B) — enum-компаратор с направлением
   (понижение = нарушение, повышение — нет); есть 3 NO_VIOLATION-примера в gold.
3. `IOS4-079` (конфигурация вентустановок) — композиционное сравнение, покрыто
   золотым VIO-0001; это единственное правило с готовой положительной парой.
4. `AR-041` (ширина эвакуационных дверей, м) — число с нижней границей 0,9 м.
5. `IOS4-078` (сечения воздуховодов) — второй золотой кластер (VIO-0003).

Покрытие всех 132 строк сейчас `extractor_missing`: компаратор описан,
исполняемого извлечения нет. Канонические коды — трёхзначные (`PZ-001`);
короткие формы из текстов (`PZ-01`, `KR-55`) — display-алиасы.
